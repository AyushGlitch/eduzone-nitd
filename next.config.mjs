/** @type {import('next').NextConfig} */

// For Development
const nextConfig = {
    images: {
        unoptimized: true,
    },
};

export default nextConfig;

// // For Production
// const nextConfig = {
//     output: 'export',
//     images: {
//         unoptimized: true,
//     }
// };

// export default nextConfig;
